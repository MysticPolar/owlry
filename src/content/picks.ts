/* ============================================================
   owlry — today's curated picks (mockup `PICKS`).
   ============================================================ */
import type { BookId } from './types';

export const PICKS: BookId[] = ['gentle', 'snow', 'piranesi'];
